{
  "name": "planejamento-semanal-saas",
  "version": "1.0.0",
  "description": "Sistema SaaS - Planejamento Semanal Educacao Infantil",
  "scripts": {
    "start": "cd backend && node src/server.js",
    "dev": "cd backend && npx nodemon src/server.js",
    "install-deps": "cd backend && npm install",
    "db:migrate": "cd backend && node src/config/migrate.js",
    "db:seed": "cd backend && node src/config/seed.js",
    "setup": "npm run install-deps && npm run db:migrate && npm run db:seed"
  }
}
